package:
  name: r-exporkit
  version: 0.9

source:
  fn: expoRkit_0.9.tar.gz
  url: http://cran.r-project.org/src/contrib/expoRkit_0.9.tar.gz

build:
  number: 0

requirements:
  build:
    - r
    - r-sparsem
  run:
    - r
    - r-sparsem

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
