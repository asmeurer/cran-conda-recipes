package:
  name: r-equivalence
  version: 0.5.7

source:
  fn: equivalence_0.5.7.tar.gz
  url: http://cran.r-project.org/src/contrib/equivalence_0.5.7.tar.gz

build:
  number: 2

requirements:
  build:
    - r
    - r-lattice
    - r-boot
  run:
    - r
    - r-lattice
    - r-boot

about:
  license: GPL-2

# vim:set ts=8 sw=2 sts=2 tw=78 et:
