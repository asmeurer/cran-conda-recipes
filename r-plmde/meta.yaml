package:
  name: r-plmde
  version: 1.0

source:
  fn: plmDE_1.0.tar.gz
  url: http://cran.r-project.org/src/contrib/plmDE_1.0.tar.gz

build:
  number: 0

requirements:
  build:
    - r
    - r-limma
    - r-r.oo
  run:
    - r
    - r-limma
    - r-r.oo

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
