package:
  name: r-missforest
  version: 1.4

source:
  fn: missForest_1.4.tar.gz
  url: http://cran.r-project.org/src/contrib/missForest_1.4.tar.gz

build:
  number: 1

requirements:
  build:
    - r
    - r-randomforest
    - r-foreach
    - r-itertools
  run:
    - r
    - r-randomforest
    - r-foreach
    - r-itertools

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
