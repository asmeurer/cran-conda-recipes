package:
  name: r-regsubseq
  version: 0.10

source:
  fn: regsubseq_0.10.tar.gz
  url: http://cran.r-project.org/src/contrib/regsubseq_0.10.tar.gz

build:
  number: 1

requirements:
  build:
    - r
  run:
    - r

about:
  license: GPL-2

# vim:set ts=8 sw=2 sts=2 tw=78 et:
