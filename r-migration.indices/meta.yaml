package:
  name: r-migration.indices
  version: 0.3.0

source:
  fn: migration.indices_0.3.0.tar.gz
  url: http://cran.r-project.org/src/contrib/migration.indices_0.3.0.tar.gz

build:
  number: 0

requirements:
  build:
    - r
    - r-calibrate
  run:
    - r
    - r-calibrate

about:
  license: AGPL-3

# vim:set ts=8 sw=2 sts=2 tw=78 et:
