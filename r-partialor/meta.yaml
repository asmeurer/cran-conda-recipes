package:
  name: r-partialor
  version: 0.9

source:
  fn: partialOR_0.9.tar.gz
  url: http://cran.r-project.org/src/contrib/partialOR_0.9.tar.gz

build:
  number: 1

requirements:
  build:
    - r
    - r-nnet
  run:
    - r
    - r-nnet

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
