package:
  name: r-fuzzyfdr
  version: 1.0

source:
  fn: fuzzyFDR_1.0.tar.gz
  url: http://cran.r-project.org/src/contrib/fuzzyFDR_1.0.tar.gz

build:
  number: 1

requirements:
  build:
    - r
  run:
    - r

about:
  license: GPL

# vim:set ts=8 sw=2 sts=2 tw=78 et:
