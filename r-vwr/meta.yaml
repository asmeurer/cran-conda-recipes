package:
  name: r-vwr
  version: 0.3.0

source:
  fn: vwr_0.3.0.tar.gz
  url: http://cran.r-project.org/src/contrib/vwr_0.3.0.tar.gz

build:
  number: 1

requirements:
  build:
    - r
    - r-stringdist
    - r-lattice
    - r-latticeextra
  run:
    - r
    - r-stringdist
    - r-lattice
    - r-latticeextra

about:
  license: GPL (>= 3)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
