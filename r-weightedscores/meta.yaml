package:
  name: r-weightedscores
  version: 0.9

source:
  fn: weightedScores_0.9.tar.gz
  url: http://cran.r-project.org/src/contrib/weightedScores_0.9.tar.gz

build:
  number: 1

requirements:
  build:
    - r
    - r-mvtnorm
    - r-rootsolve
  run:
    - r
    - r-mvtnorm
    - r-rootsolve

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
