package:
  name: r-fma
  version: 2.01

source:
  fn: fma_2.01.tar.gz
  url: http://cran.r-project.org/src/contrib/fma_2.01.tar.gz

build:
  number: 1

requirements:
  build:
    - r
    - r-tseries
    - r-forecast
  run:
    - r
    - r-tseries
    - r-forecast

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
