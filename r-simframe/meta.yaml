package:
  name: r-simframe
  version: 0.5.1

source:
  fn: simFrame_0.5.1.tar.gz
  url: http://cran.r-project.org/src/contrib/simFrame_0.5.1.tar.gz

build:
  number: 1

requirements:
  build:
    - r
    - r-rcpp
    - r-lattice
  run:
    - r
    - r-rcpp
    - r-lattice

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
