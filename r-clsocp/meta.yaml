package:
  name: r-clsocp
  version: 1.0

source:
  fn: CLSOCP_1.0.tar.gz
  url: http://cran.r-project.org/src/contrib/CLSOCP_1.0.tar.gz

build:
  number: 1

requirements:
  build:
    - r
    - r-matrix
  run:
    - r
    - r-matrix

about:
  license: GPL-3

# vim:set ts=8 sw=2 sts=2 tw=78 et:
