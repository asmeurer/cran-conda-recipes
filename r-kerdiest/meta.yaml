package:
  name: r-kerdiest
  version: 1.2

source:
  fn: kerdiest_1.2.tar.gz
  url: http://cran.r-project.org/src/contrib/kerdiest_1.2.tar.gz

build:
  number: 0

requirements:
  build:
    - r-date
    - r-chron
    - r-evir
  run:
    - r-date
    - r-chron
    - r-evir

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
