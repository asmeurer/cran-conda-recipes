package:
  name: r-optmatch
  version: 0.9.1

source:
  fn: optmatch_0.9-1.tar.gz
  url: http://cran.r-project.org/src/contrib/optmatch_0.9-1.tar.gz

build:
  number: 1

requirements:
  build:
    - r
    - r-digest
  run:
    - r
    - r-digest

about:
  license: file LICENSE

# vim:set ts=8 sw=2 sts=2 tw=78 et: