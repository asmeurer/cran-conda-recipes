package:
  name: r-outliers
  version: 0.14

source:
  fn: outliers_0.14.tar.gz
  url: http://cran.r-project.org/src/contrib/outliers_0.14.tar.gz

build:
  number: 0

requirements:
  build:
    - r
  run:
    - r

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
