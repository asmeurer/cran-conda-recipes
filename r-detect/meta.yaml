package:
  name: r-detect
  version: 0.3.1

source:
  fn: detect_0.3-1.tar.gz
  url: http://cran.r-project.org/src/contrib/detect_0.3-1.tar.gz

build:
  number: 1

requirements:
  build:
    - r
    - r-formula
  run:
    - r
    - r-formula

about:
  license: GPL-2

# vim:set ts=8 sw=2 sts=2 tw=78 et: