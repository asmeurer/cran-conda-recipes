package:
  name: r-rivivc
  version: 0.9

source:
  fn: Rivivc_0.9.tar.gz
  url: http://cran.r-project.org/src/contrib/Rivivc_0.9.tar.gz

build:
  number: 0

requirements:
  build:
    - r
    - r-signal
  run:
    - r
    - r-signal

about:
  license: GPL (>= 3)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
