package:
  name: r-fticrms
  version: 0.8

source:
  fn: FTICRMS_0.8.tar.gz
  url: http://cran.r-project.org/src/contrib/FTICRMS_0.8.tar.gz

build:
  number: 1

requirements:
  build:
    - r
    - r-matrix
    - r-lattice
  run:
    - r
    - r-matrix
    - r-lattice

about:
  license: GPL-2

# vim:set ts=8 sw=2 sts=2 tw=78 et:
