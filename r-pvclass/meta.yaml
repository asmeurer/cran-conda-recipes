package:
  name: r-pvclass
  version: 1.0

source:
  fn: pvclass_1.0.tar.gz
  url: http://cran.r-project.org/src/contrib/pvclass_1.0.tar.gz

build:
  number: 1

requirements:
  build:
    - r
    - r-matrix
  run:
    - r
    - r-matrix

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
