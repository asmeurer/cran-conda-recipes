package:
  name: r-daks
  version: 2.1.2

source:
  fn: DAKS_2.1-2.tar.gz
  url: http://cran.r-project.org/src/contrib/DAKS_2.1-2.tar.gz

build:
  number: 1

requirements:
  build:
    - r
    - r-relations
    - r-sets
  run:
    - r
    - r-relations
    - r-sets

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et: