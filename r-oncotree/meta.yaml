package:
  name: r-oncotree
  version: 0.3.3

source:
  fn: Oncotree_0.3.3.tar.gz
  url: http://cran.r-project.org/src/contrib/Oncotree_0.3.3.tar.gz

build:
  number: 1

requirements:
  build:
    - r
    - r-boot
  run:
    - r
    - r-boot

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
