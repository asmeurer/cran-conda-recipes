package:
  name: r-tikzdevice
  version: 0.7.0

source:
  fn: tikzDevice_0.7.0.tar.gz
  url: http://cran.r-project.org/src/contrib/tikzDevice_0.7.0.tar.gz

build:
  number: 0

requirements:
  build:
    - r
    - r-filehash
  run:
    - r
    - r-filehash

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et: