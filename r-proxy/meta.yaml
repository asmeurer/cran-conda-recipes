package:
  name: r-proxy
  version: 0.4.10

source:
  fn: proxy_0.4-10.tar.gz
  url: http://cran.r-project.org/src/contrib/proxy_0.4-10.tar.gz

build:
  number: 1

requirements:
  build:
    - r
  run:
    - r

about:
  license: GPL-2

# vim:set ts=8 sw=2 sts=2 tw=78 et: