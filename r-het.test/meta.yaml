package:
  name: r-het.test
  version: 0.1

source:
  fn: het.test_0.1.tar.gz
  url: http://cran.r-project.org/src/contrib/het.test_0.1.tar.gz

build:
  number: 1

requirements:
  build:
    - r
    - r-vars
  run:
    - r
    - r-vars

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
