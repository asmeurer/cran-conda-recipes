package:
  name: r-rcmdrplugin.lfstat
  version: 0.7

source:
  fn: RcmdrPlugin.lfstat_0.7.tar.gz
  url: http://cran.r-project.org/src/contrib/RcmdrPlugin.lfstat_0.7.tar.gz

build:
  number: 0

requirements:
  build:
    - r
    - r-lfstat
    - r-rcmdr
  run:
    - r
    - r-lfstat
    - r-rcmdr

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
