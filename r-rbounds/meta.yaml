package:
  name: r-rbounds
  version: 0.9

source:
  fn: rbounds_0.9.tar.gz
  url: http://cran.r-project.org/src/contrib/rbounds_0.9.tar.gz

build:
  number: 0

requirements:
  build:
    - r
    - r-matching
  run:
    - r
    - r-matching

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
