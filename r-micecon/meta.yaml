package:
  name: r-micecon
  version: 0.6.10

source:
  fn: micEcon_0.6-10.tar.gz
  url: http://cran.r-project.org/src/contrib/micEcon_0.6-10.tar.gz

build:
  number: 1

requirements:
  build:
    - r
    - r-misctools
  run:
    - r
    - r-misctools

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et: