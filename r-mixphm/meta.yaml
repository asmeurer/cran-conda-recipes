package:
  name: r-mixphm
  version: 0.7.0

source:
  fn: mixPHM_0.7.0.tar.gz
  url: http://cran.r-project.org/src/contrib/mixPHM_0.7.0.tar.gz

build:
  number: 2

requirements:
  build:
    - r
    - r-survival
    - r-lattice
  run:
    - r
    - r-survival
    - r-lattice

about:
  license: GPL

# vim:set ts=8 sw=2 sts=2 tw=78 et:
