package:
  name: r-rcmdrplugin.sampling
  version: 1.1

source:
  fn: RcmdrPlugin.sampling_1.1.tar.gz
  url: http://cran.r-project.org/src/contrib/RcmdrPlugin.sampling_1.1.tar.gz

build:
  number: 0

requirements:
  build:
    - r
    - r-lpsolve
    - r-sampling
    - r-rcmdr
  run:
    - r
    - r-lpsolve
    - r-sampling
    - r-rcmdr

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
