package:
  name: r-rpmm
  version: 1.10

source:
  fn: RPMM_1.10.tar.gz
  url: http://cran.r-project.org/src/contrib/RPMM_1.10.tar.gz

build:
  number: 1

requirements:
  build:
    - r
    - r-cluster
  run:
    - r
    - r-cluster

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
