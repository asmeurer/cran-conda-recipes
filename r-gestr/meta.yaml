package:
  name: r-gestr
  version: 0.1

source:
  fn: GESTr_0.1.tar.gz
  url: http://cran.r-project.org/src/contrib/GESTr_0.1.tar.gz

build:
  number: 1

requirements:
  build:
    - r
    - r-mclust
    - r-gtools
  run:
    - r
    - r-mclust
    - r-gtools

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
