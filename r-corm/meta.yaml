package:
  name: r-corm
  version: 1.0.0

source:
  fn: CORM_1.0.0.tar.gz
  url: http://cran.r-project.org/src/contrib/CORM_1.0.0.tar.gz

build:
  number: 1

requirements:
  build:
    - r
    - r-cluster
  run:
    - r
    - r-cluster

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
