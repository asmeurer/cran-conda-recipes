package:
  name: r-relaxo
  version: 0.1.2

source:
  fn: relaxo_0.1-2.tar.gz
  url: http://cran.r-project.org/src/contrib/relaxo_0.1-2.tar.gz

build:
  number: 0

requirements:
  build:
    - r-lars
  run:
    - r-lars

about:
  license: GPL

# vim:set ts=8 sw=2 sts=2 tw=78 et: