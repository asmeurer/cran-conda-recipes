package:
  name: r-sampling
  version: 2.6

source:
  fn: sampling_2.6.tar.gz
  url: http://cran.r-project.org/src/contrib/sampling_2.6.tar.gz

build:
  number: 0

requirements:
  build:
    - r
    - r-mass
    - r-lpsolve
  run:
    - r
    - r-mass
    - r-lpsolve

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
