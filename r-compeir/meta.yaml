package:
  name: r-compeir
  version: 1.0

source:
  fn: compeir_1.0.tar.gz
  url: http://cran.r-project.org/src/contrib/compeir_1.0.tar.gz

build:
  number: 1

requirements:
  build:
    - r
    - r-lattice
    - r-etm
  run:
    - r
    - r-lattice
    - r-etm

about:
  license: GPL-2

# vim:set ts=8 sw=2 sts=2 tw=78 et:
