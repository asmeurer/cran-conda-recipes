package:
  name: r-bisoreg
  version: 1.1

source:
  fn: bisoreg_1.1.tar.gz
  url: http://cran.r-project.org/src/contrib/bisoreg_1.1.tar.gz

build:
  number: 0

requirements:
  build:
    - r
    - r-bootstrap
    - r-monreg
    - r-r2winbugs
    - r-coda
  run:
    - r
    - r-bootstrap
    - r-monreg
    - r-r2winbugs
    - r-coda

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
