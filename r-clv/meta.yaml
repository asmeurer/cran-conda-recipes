package:
  name: r-clv
  version: 0.3.2.1

source:
  fn: clv_0.3-2.1.tar.gz
  url: http://cran.r-project.org/src/contrib/clv_0.3-2.1.tar.gz

build:
  number: 2

requirements:
  build:
    - r
    - r-cluster
    - r-class
  run:
    - r
    - r-cluster
    - r-class

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
