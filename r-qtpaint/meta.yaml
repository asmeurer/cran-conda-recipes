package:
  name: r-qtpaint
  version: 0.9.0

source:
  fn: qtpaint_0.9.0.tar.gz
  url: http://cran.r-project.org/src/contrib/qtpaint_0.9.0.tar.gz

build:
  number: 2

requirements:
  build:
    - r
    - r-qtbase
    - r-qtbase
  run:
    - r
    - r-qtbase
    - r-qtbase

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
