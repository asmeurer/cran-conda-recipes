package:
  name: r-fpca
  version: 0.2.1

source:
  fn: fpca_0.2-1.tar.gz
  url: http://cran.r-project.org/src/contrib/fpca_0.2-1.tar.gz

build:
  number: 0

requirements:
  build:
    - r-sm
  run:
    - r-sm

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et: