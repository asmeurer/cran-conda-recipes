package:
  name: r-subselect
  version: 0.12.3

source:
  fn: subselect_0.12-3.tar.gz
  url: http://cran.r-project.org/src/contrib/subselect_0.12-3.tar.gz

build:
  number: 0

requirements:
  build:
  run:

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et: