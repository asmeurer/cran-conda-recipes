package:
  name: r-calibrator
  version: 1.2.6

source:
  fn: calibrator_1.2-6.tar.gz
  url: http://cran.r-project.org/src/contrib/calibrator_1.2-6.tar.gz

build:
  number: 0

requirements:
  build:
    - r
    - r-emulator
    - r-cubature
  run:
    - r
    - r-emulator
    - r-cubature

about:
  license: GPL

# vim:set ts=8 sw=2 sts=2 tw=78 et: