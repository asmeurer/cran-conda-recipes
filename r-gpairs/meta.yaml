package:
  name: r-gpairs
  version: 1.1

source:
  fn: gpairs_1.1.tar.gz
  url: http://cran.r-project.org/src/contrib/gpairs_1.1.tar.gz

build:
  number: 0

requirements:
  build:
    - r-barcode
    - r-vcd
    - r-colorspace
  run:
    - r-barcode
    - r-vcd
    - r-colorspace

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
