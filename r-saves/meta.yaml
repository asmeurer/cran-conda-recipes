package:
  name: r-saves
  version: 0.5

source:
  fn: saves_0.5.tar.gz
  url: http://cran.r-project.org/src/contrib/saves_0.5.tar.gz

build:
  number: 0

requirements:
  build:
    - r
  run:
    - r

about:
  license: AGPL-3

# vim:set ts=8 sw=2 sts=2 tw=78 et:
