package:
  name: r-cvst
  version: 0.2.1

source:
  fn: CVST_0.2-1.tar.gz
  url: http://cran.r-project.org/src/contrib/CVST_0.2-1.tar.gz

build:
  number: 2

requirements:
  build:
    - r
    - r-kernlab
    - r-matrix
  run:
    - r
    - r-kernlab
    - r-matrix

about:
  license: GPL (>= 2.0)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
