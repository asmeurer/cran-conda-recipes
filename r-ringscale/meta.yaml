package:
  name: r-ringscale
  version: 0.1.2

source:
  fn: ringscale_0.1.2.tar.gz
  url: http://cran.r-project.org/src/contrib/ringscale_0.1.2.tar.gz

build:
  number: 0

requirements:
  build:
    - r-fitsio
  run:
    - r-fitsio

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et: