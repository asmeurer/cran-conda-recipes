package:
  name: r-ebs
  version: 2.8

source:
  fn: EBS_2.8.tar.gz
  url: http://cran.r-project.org/src/contrib/EBS_2.8.tar.gz

build:
  number: 0

requirements:
  build:
    - r
  run:
    - r

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
