package:
  name: r-mopsocd
  version: 0.5.1

source:
  fn: mopsocd_0.5.1.tar.gz
  url: http://cran.r-project.org/src/contrib/mopsocd_0.5.1.tar.gz

build:
  number: 1

requirements:
  build:
    - r
  run:
    - r

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
