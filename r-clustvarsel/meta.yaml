package:
  name: r-clustvarsel
  version: 2.0

source:
  fn: clustvarsel_2.0.tar.gz
  url: http://cran.r-project.org/src/contrib/clustvarsel_2.0.tar.gz

build:
  number: 1

requirements:
  build:
    - r
    - r-mclust
    - r-bma
    - r-foreach
    - r-iterators
  run:
    - r
    - r-mclust
    - r-bma
    - r-foreach
    - r-iterators

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
