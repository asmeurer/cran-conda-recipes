package:
  name: r-ppstat
  version: 0.9

source:
  fn: ppstat_0.9.tar.gz
  url: http://cran.r-project.org/src/contrib/ppstat_0.9.tar.gz

build:
  number: 1

requirements:
  build:
    - r
    - r-processdata
    - r-reshape2
    - r-ggplot2
    - r-matrix
  run:
    - r
    - r-processdata
    - r-reshape2
    - r-ggplot2
    - r-matrix

about:
  license: GPL (>= 2)

# vim:set ts=8 sw=2 sts=2 tw=78 et:
